pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Hello Team, This is Demo build updated..."
                /*sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''*/
            }
        }
    }
}
