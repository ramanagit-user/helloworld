pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Hello Team, This is a Demo build"
                /*sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''*/
            }
        }
    }
}
